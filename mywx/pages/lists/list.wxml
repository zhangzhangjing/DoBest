<map id="myMap"
      longitude="{{longitude}}" latitude="{{latitude}}" 
      markers="{{markers}}"
      scale="20" 
      style="width:{{map_width}}px;height:{{map_height}}px"
      bindregionchange="regionchange"
      controls="{{controls}}"
      show-location class='mapitem'>
</map>